#!/usr/bin/env bash

lua_psl_version="0.3-0"

set -e -u -x
source ./tasks/helpers.sh

"${LUAROCKS_CMD[@]}" --tree="$APP_CORE_VENDOR_LUA_DIR" install "psl" "$lua_psl_version" PSL_DIR="$STAGE_EMBEDDED_DIR"
find "$APP_CORE_VENDOR_LUA_DIR/lib" -name "psl.so" -exec chrpath -d {} \;

stamp
