dependencies:
  cache_directories:
    - build
    - node_modules
    - vendor
test:
  override:
    - make install PREFIX="$(pwd)/build"
    - make clean
    - API_UMBRELLA_ROOT="$(pwd)/build" npm test
