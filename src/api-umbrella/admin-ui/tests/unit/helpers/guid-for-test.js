import { guidFor } from 'api-umbrella-admin-ui/helpers/guid-for';
import { module, test } from 'qunit';

module('Unit | Helper | guid for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = guidFor([42]);
  assert.ok(result);
});
