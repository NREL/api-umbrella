import { inflect } from 'api-umbrella-admin-ui/helpers/inflect';
import { module, test } from 'qunit';

module('Unit | Helper | inflect');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inflect([42]);
  assert.ok(result);
});
