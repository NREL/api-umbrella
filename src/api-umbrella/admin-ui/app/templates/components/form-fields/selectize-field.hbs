<FormFields::FieldWrapper @model={{this.model}} @style={{this.style}} @fieldName={{this.fieldName}} @inputId={{this.inputId}} @labelForId={{this.selectizeTextInputId}} @label={{this.label}} @tooltip={{this.tooltip}} @hint={{this.hint}} {{did-insert this.didInsert}}>
  <input type="text" value={{get this.model this.fieldName}} id={{this.inputId}} onchange={{action (mut (get this.model this.fieldName)) value="target.value"}}>
</FormFields::FieldWrapper>
