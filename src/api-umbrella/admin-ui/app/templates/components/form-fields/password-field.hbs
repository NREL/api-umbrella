<FormFields::FieldWrapper @model={{this.model}} @style={{this.style}} @fieldName={{this.fieldName}} @inputId={{this.inputId}} @label={{this.label}} @tooltip={{this.tooltip}} @hint={{this.hint}}>
  <input type="password" value={{get this.model this.fieldName}} id={{this.inputId}} class="form-control" placeholder={{this.placeholder}} oninput={{action (mut (get this.model this.fieldName)) value="target.value"}}>
</FormFields::FieldWrapper>
