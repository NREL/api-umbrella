<NativeSelectBox @value={{@value}} @onSelect={{this.handleSelect}} id={{@inputId}} class={{@inputClass}} as |sb|>
  {{#each @options as |opt|}}
    <sb.Option @value={{opt.id}}>{{opt.name}}</sb.Option>
  {{/each}}
</NativeSelectBox>