var CommonValidations = {
  host_format: new RegExp(<%= CommonValidations::HOST_FORMAT.source.to_json %>),
  url_prefix_format: new RegExp(<%= CommonValidations::URL_PREFIX_FORMAT.source.to_json %>)
};
