en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      html: '<abbr title="Required" class="required"><span class="abbr-required">*</span></abbr>'
    error_notification:
      default_message: "Some errors were found, please take a look:"
    # Labels and hints examples
    # labels:
    #   password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail para efetuar o sign in.'
    #     edit:
    #       email: 'E-mail.'
    labels:
      api_user:
        first_name: "First Name"
        last_name: "Last Name"
        website: "Website"
        use_description: "How will you use the APIs?<br />(optional)"
        throttle_hourly_limit: Hourly rate limit
        throttle_daily_limit: Daily rate limit
        throttle_mode: Rate limits
        throttle_by_ip: Limit By
      api_doc_service:
        http_method: "HTTP Method"
        api_doc_collection_id: "Documentation Collection Page"
    placeholders:
      api_user:
        website: "http://"
      api_doc_service:
        path: "/api/example/service"
    hints:
      api_doc_service:
        path: "The base path to the web service endpoint."
        summary: "A short description of this service (1-2 sentences)."
    options:
      api_user:
        throttle_mode:
          default: Default rate limits (1,000 per hour, 10,000 per day)
          unthrottled: Unlimited requests
          custom: Custom rate limits
        throttle_by_ip:
          "true": "Rate limit by IP address"
          "false": "Rate limit by API key"
  helpers:
    submit:
      api_user:
        create: "Sign Up"
