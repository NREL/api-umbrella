# Authentication Backend
#
# These backend proxy servers authenticate all the /api requests.
backend auth_proxy_farm-<%= deploy_name %>
  balance roundrobin
  <% server_registry[:auth_proxy].each_with_index do |server, i| %>
  <%= "server auth_proxy#{i}-#{deploy_name} #{server[:host]}:#{server[:port]}" %>
  <% end %>
