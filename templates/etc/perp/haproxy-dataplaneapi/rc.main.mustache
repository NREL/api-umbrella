#!/usr/bin/env bash
set -e -u

# Redirect stderr to stdout
exec 2>&1

umask "{{umask}}"

if [ "${1}" = "start" ]; then
  echo "starting ${2}..."

  run_args=("-e" "rc.env")
  exec runtool "${run_args[@]}" dataplaneapi -f "{{etc_dir}}/haproxy/dataplaneapi.hcl"
fi

exit 0
