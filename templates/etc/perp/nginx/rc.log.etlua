#!/usr/bin/env bash
set -e -u

if [ "${1}" = "start" ]; then
  service="$2"
  destination="<%- config['log']['destination'] %>"
  if [ "$destination" = "console" ]; then
    ln -sf /dev/stdout "<%- config['log_dir'] %>/$service/access.log"
  fi
fi

exec ../rc.log "$@"
