#!/bin/sh

# Redirect stderr to stdout
exec 2>&1

if test ${1} = "start"; then
  echo "starting ${2}..."
  exec varnishncsa \
    -a \
    -w {{log_dir}}/varnishncsa.log \
    -n {{run_dir}}/varnish/api-umbrella
fi

exit 0
