#!/bin/sh

# Redirect stderr to stdout
exec 2>&1

if test ${1} = "start"; then
  echo "starting ${2}..."
  exec runtool -e rc.env -c {{_src_root_dir}} nginx -p {{_src_root_dir}}/ -c {{etc_dir}}/nginx/router.conf
fi

exit 0
