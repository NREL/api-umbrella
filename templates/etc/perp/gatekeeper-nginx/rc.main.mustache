#!/bin/sh

# Redirect stderr to stdout
exec 2>&1

if test ${1} = "start"; then
  echo "starting ${2}..."
  exec runtool -e rc.env -c {{gatekeeper.dir}} nginx -p {{gatekeeper.dir}}/ -c {{gatekeeper.dir}}/conf/nginx.conf
fi

exit 0
