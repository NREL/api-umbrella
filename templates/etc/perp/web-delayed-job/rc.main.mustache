#!/bin/sh

# Redirect stderr to stdout
exec 2>&1

if test ${1} = "start"; then
  echo "starting ${2}..."
  exec runtool -e rc.env -c {{gatekeeper.dir}}/src/api-umbrella/web-app bundle exec ./script/delayed_job --pid-dir={{run_dir}} --queues=mailers run
fi

exit 0
