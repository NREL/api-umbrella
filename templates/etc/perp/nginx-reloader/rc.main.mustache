#!/bin/sh

# Redirect stderr to stdout
exec 2>&1

if test ${1} = "start"; then
  echo "starting ${2}..."

  RUN_OPTS="-e rc.env"
  exec runtool $RUN_OPTS {{_src_root_dir}}/bin/api-umbrella-nginx-reloader
fi

exit 0
